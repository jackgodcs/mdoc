# Security

请通过 GitHub Security Advisory 私下报告漏洞，不要在公开 Issue 中提交凭据、正式手册内容、绝对路径或诊断包。

mdoc 默认零遥测；本地 PDF 查看服务只监听 `127.0.0.1`。联网下载必须逐次可见并获得用户同意。诊断默认仅保留最新一份经过脱敏和限长处理的本地记录，不自动上传。

Stable Release 资产必须通过 SHA-256、Release Manifest、泄漏扫描、测试和许可证检查。
