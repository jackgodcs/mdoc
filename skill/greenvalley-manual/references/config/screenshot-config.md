# screenshots.yaml

Records screenshot requirements and acceptance state. Use stable screenshot IDs and English filenames. Each task declares pages, locale policy, entry steps, preconditions, expected state, sensitivity rules, capture state, file mapping, and review state.

Do not mark approved when files are missing. Do not use approved status to authorize overwriting an unrelated existing file.

