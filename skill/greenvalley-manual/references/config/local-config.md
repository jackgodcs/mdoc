# Local Override Files

workspace.local.yaml stores machine-specific application paths, local backup/development settings, and capture temporary paths. task.local.yaml stores task-specific source-package and example-data paths.

These files override compatible shared fields and should not be placed in transferable examples or formal manuals. Never store passwords, license keys, or tokens. A missing local file is allowed until a phase needs its values.

