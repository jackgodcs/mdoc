# terminology.csv

Columns: term_id, source, locale columns, ui_label, keep_original, notes. Use one stable term ID per concept. UI labels should reflect the live localized interface. Mark technical names such as SDK/package identifiers with keep_original=true when they must not be translated.

